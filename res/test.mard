//TODO make import working | import are mocked for now and only core module is always loaded

mymax a: int, b: int do
  ret
    if a > b then
      a
    else
      b

Main -> int do
  Serial.begin(9600)
  pinMode(13, OUTPUT)
  i: int = 0
  top: int = 5
  while i < top do
    tak: int = 30
    i = i + 1
    Serial.println(F("Tak czy siak"))
  while true do
    printMemStats()
    digitalWrite(13, LOW)
    Serial.println(F("LOW"))
    sleep(1000)
    digitalWrite(13, HIGH)
    Serial.println(F("HIGH"))
    sleep(500)
    digitalWrite(13, LOW)
    Serial.println("LOW")
    sleep(500)
    digitalWrite(13, HIGH)
    Serial.println("HIGH")
    sleep(2000)
    tak = 12
    Serial.println(mymax(tak, 15))
    myList = List<int>()
    myList.add(10)
    res = myList.get(0)
    myList2 = [(1+1), 1, 3]
    Serial.println(res)
    classTest: ThisTest = ThisTest(12, 100)
    Serial.println(classTest.a)
  ret 0


