import Ala.Ma.Kota
import XD

class Name:
    b: int = 12
    c = "string"
    Name do
        ret 1
    test do
        ret 1


dupa t1: Name, t2: int -> int do
    b = t1
    ret b1

//f1 ala: string, ma: int, kota: float do
//    f2 b do
//        b: float = 10.12
//        dupa: float = (12.12 + 100) / 12
//    f2(12)

f3 x: int do
    a = "alla test string xD"
    c: int = {\x: int, y: int, z: int ->
                k = 12
                ret x + y
             }
    c(1, 2, 3)
    res = f1(xasd, (12 / 4), (\x -> "a"), 12)
    test = if true then
            a: int = "12"
            b: string = "allah"

        elif false then
            a: int = 666

        else
            x: int = 12
            myList = [1, 2.2, 3 / 31, 4]


main do
    myList = [1, 2.2, 3 / 31, 4]
    if true then
        ret 0
    else
        ret 0
    while true do
        loop()


loop do
    for z <- [1..2] do
        ret 1
    name().dupa()

